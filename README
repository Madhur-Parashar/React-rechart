Clone the repository using the link.
Install the dependencies using "npm install" or "yarn install"
Run the project through "npm run" or "yarn start"
---------------------------------------------------------------------------------------------------------------------------------
